# Issue #225 — short-context handoff

## 目的

PR #227 の J-Quants 実データ OOS 証拠を確定し、Issue #225 を完了させる。

このタスクではモデル改善をしない。`market + momentum + volatility` の現モデルが baseline を上回ったかどうかを、保存済み実測結果どおりに確定するだけ。

## 正本

- Issue: #225
- PR: #227
- Branch: `research/skfolio-jquants-oos`
- 実測結果: `docs/research/results/skfolio_characteristics_jquants_free/summary.json`

GitHub の現在の PR head / CI を過去の会話より優先する。

## 最初に読むファイル

この順で読む。最初から他ファイルを走査しない。

1. `docs/research/results/skfolio_characteristics_jquants_free/summary.json`
2. `.github/workflows/skfolio-jquants-oos.yml`
3. `scripts/skfolio_jquants_oos.py` — 計算やCI失敗を調べる時だけ
4. `src/research/skfolio_characteristics.py` — モデル契約を調べる時だけ

`fold*.npz` は通常読まない。hash検証や数値デバッグが必要な時だけ使う。

## 確定済み実測

同じ64銘柄・同じ単純収益率・2-fold OOSで比較済み。

- baseline normalized Frobenius error: `0.5707291937906769`
- skfolio normalized Frobenius error: `0.6540009162289466`
- baseline equal-weight volatility absolute error: `0.021667940442276046`
- skfolio equal-weight volatility absolute error: `0.027719332919688117`
- verdict: `empirical_baseline_better_or_equal_on_both_primary_risk_metrics`

結論: 現在の skfolio characteristics covariance は baseline を置き換えない。

これは risk/covariance forecast の結果であり、alpha・期待収益・戦略収益の改善を意味しない。

## 次の1手

PR #227 の**現在のhead**に対するCIを確認する。

- relevant CI がすべて成功
- 一時診断workflowが差分に残っていない
- 保存済み `summary.json` の verdict が上記と一致

を満たしたら、PR #227 を merge し、`main` から結果ファイルをread-backして Issue #225 を完了する。

CIが失敗している場合だけ、その失敗1件を直す。モデル変更には進まない。

## 禁止事項

- skfolioがbaselineを上回ったと書き換えない。
- このタスク内で factor を追加・調整しない。
- AlphaZeroBeta の log-return field を skfolio momentum に流用しない。
- price、volume、price*volume を market capitalization の代理にしない。
- raw J-Quants rows をGitHub成果物へ保存しない。
- fallbackで欠損やCI失敗を隠さない。
- 一時診断workflowを完了PRに残さない。

## 完了条件

- [ ] PR #227 の exact current head で必要CIが成功
- [ ] PR #227 が merge 済み
- [ ] `main` から `summary.json` をread-backして同じ verdict を確認
- [ ] Issue #225 に実測値・merge SHA・claim boundary を記録
- [ ] Issue #225 を close

## 別Issueにするもの

J-Quants の `MktCap` を使った Size/Beta、時価総額加重、追加factorの比較は #225 に混ぜない。独立した次実験として扱う。
