"""External read-only provider clients.

Provider modules own upstream HTTP contracts. They do not decide whether data is
accepted into the canonical input ledger or promoted into research evidence.
"""
