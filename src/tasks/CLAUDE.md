# src/tasks/ CLAUDE.md

ここは「特別な司令」を遂行する裏方の部屋である。統計の収集、バッチ処理の実行、及び独立した業務の遂行を担当する。

---

## 関心の分離 (Responsibility)
- **役割**: 定期的なバックアップ、統計の集計、ワンショットのスクリプト実行。
- **含まれるもの**: `stats.ts`（キャッシュ統計の生成）など。

## 禁止事項 (Strict Rules)
- **循環依存の回避**: 他のディレクトリを利用することは許容されるが、互いに呼び出し合う循環参照とならないよう、コマンドライン引数等で制御する。

## 検証手順 (Verification)
- `bun run src/tasks/stats.ts` を実行して、期待通りのファイルおよび出力が得られることを確認する。